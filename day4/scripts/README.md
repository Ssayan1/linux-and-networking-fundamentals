# Day 4 Scripts  
This folder contains CPU load generators used for debugging labs.

- cpu-burner.sh → Single-threaded CPU load  
- cpu-stress.sh → Multi-threaded CPU stress tool  

All scripts are safe to run inside WSL.
