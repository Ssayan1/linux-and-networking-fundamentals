# LAB 1 — Build Your First Python Image

1. Go to the example folder:

```
cd day6/examples/python-app
```

2. Build the image:

```
docker build -t python-hello .
```

3. Run the container:

```
docker run --rm python-hello
```
