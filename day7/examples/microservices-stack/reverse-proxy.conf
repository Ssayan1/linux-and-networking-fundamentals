server {
    listen 80;

    location /api/ {
    proxy_pass http://api:5001/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
}

    location / {
        proxy_pass http://frontend:80/;
    }
}

